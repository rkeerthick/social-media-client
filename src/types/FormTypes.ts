export type initialValuesRegisterTypes = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
  location: string;
  occupation: string;
  picture: string;
  picturePath: string;
};

export type initialValuesLoginTypes = {
    email: string;
    password: string;
}